import { createFileRoute } from "@tanstack/react-router"; import { useQuery } from "@tanstack/react-query"; import { Check, Copy, Quote, Share2 } from "lucide-react"; import { useState } from "react"; import toast from "react-hot-toast"; import { AppShell } from "@/components/app-shell"; import { RouteGuard } from "@/components/route-guard"; import { Button } from "@/components/ui/button"; import { answerDateLabel, loadStoryData } from "@/lib/historian";
export const Route = createFileRoute("/memories")({ component: () => <RouteGuard><AppShell><MemoriesPage /></AppShell></RouteGuard>, head: () => ({ meta: [{ title: "Lessons & Memories — Personal Historian" }, { name: "description", content: "Revisit the lessons and milestones found throughout your life story." }] }) });
function MemoriesPage() { const query = useQuery({ queryKey: ["story-data"], queryFn: loadStoryData }); const [copied,setCopied] = useState<number|null>(null); const analyses = (query.data?.analyses ?? []).filter((item) => item.lesson); const answerMap = new Map((query.data?.answers ?? []).map((a) => [a.id,a])); const copy = async (id:number,text:string) => { await navigator.clipboard.writeText(text); setCopied(id); toast.success("Lesson copied."); setTimeout(() => setCopied(null),1500); }; return <div><p className="text-sm font-semibold uppercase tracking-[.15em] text-primary">What your life has taught you</p><h1 className="mt-2 font-serif text-4xl font-semibold sm:text-5xl">Lessons worth carrying forward</h1><p className="mt-3 max-w-2xl text-muted-foreground">Small truths, hard-won wisdom, and moments that changed your direction.</p>{query.isLoading ? <div className="mt-10 grid gap-5 md:grid-cols-2"><div className="h-52 animate-pulse rounded-3xl bg-muted" /><div className="h-52 animate-pulse rounded-3xl bg-muted" /></div> : analyses.length ? <div className="mt-10 grid gap-5 md:grid-cols-2">{analyses.map((analysis,index) => { const answer = answerMap.get(analysis.answer_id); return <article key={analysis.id} className="flex min-h-56 flex-col rounded-3xl border bg-card p-7"><Quote className="size-8 text-primary/40" /><blockquote className="mt-5 flex-1 font-serif text-2xl leading-relaxed">{analysis.lesson}</blockquote><div className="mt-7 flex items-center justify-between gap-3"><span className="text-xs text-muted-foreground">{answer ? answerDateLabel(answer.assigned_date) : `Memory ${index+1}`}{analysis.is_milestone ? " · Milestone" : ""}</span><div className="flex"><Button variant="ghost" size="icon" aria-label="Copy lesson" onClick={() => copy(analysis.id,analysis.lesson ?? "")}>{copied === analysis.id ? <Check /> : <Copy />}</Button><Button variant="ghost" size="icon" aria-label="Share lesson" onClick={() => window.open(`https://twitter.com/intent/tweet?text=${encodeURIComponent(`“${analysis.lesson}”`)}`,"_blank","noopener,noreferrer")}><Share2 /></Button></div></div></article>; })}</div> : <div className="mt-12 rounded-3xl border bg-card p-12 text-center"><Quote className="mx-auto text-primary" /><h2 className="mt-5 font-serif text-3xl">Your lessons will gather here</h2><p className="mx-auto mt-3 max-w-md text-muted-foreground">As you answer daily questions, the insights woven through your memories will become easier to see.</p></div>}</div>; }